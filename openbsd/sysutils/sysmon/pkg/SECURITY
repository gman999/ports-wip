$OpenBSD$

${WRKDIR}/client.c
  use of strcpy instead of strlcpy

${WRKDIR}/lib.c
  use of vsprintf instead of vsnprintf
  rand may return a deterministic value

${WRKDIR}/syswatch.c
  use of strcpy instead of strlcpy

${WRKDIR}/page.c
  use of strcat instead of strlcat
  use of sprintf instead of snprintf
